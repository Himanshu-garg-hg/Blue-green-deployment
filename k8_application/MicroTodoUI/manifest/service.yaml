apiVersion: v1
kind: service
metadata:
  name: microtodo-ui-service
  labels:
    app: microtodo-ui
spec:
  type: LoadBalancer
  selector:
    app: microtodo-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80