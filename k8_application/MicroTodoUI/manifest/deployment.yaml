apiVersion: apps/v1
kind: Deployment
metadata:
  name: microtodo-ui-deployment
  labels:
    app: microtodo-ui
spec:
   replicas: 2
   selector:
     matchLabels:
        app: microtodo-ui
   template:
     metadata:
       name: microtodo-ui-pod
       labels:
         app: microtodo-ui
     spec:
       containers:
         - name: microtodo-ui-container
           image: k8acrregistrydev.azurecr.io/microtodoui:latest
           ports:
             - containerPort: 80
           resources:
             requests:
               cpu: "100m"
               memory: "128Mi"
             limits:
               cpu: "250m"
               memory: "256Mi"