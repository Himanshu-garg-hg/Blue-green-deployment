apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.environment }}-addtask-todo-microservice-pod
  labels:
    app: {{ .Values.environment }}-addtask-todo-microservice
spec:
  selector:
    matchLabels:
      app: {{ .Values.environment }}-addtask-todo-microservice
  replicas: {{ .Values.addtask.replicas }}
  template:
    metadata:
      labels:
        app: {{ .Values.environment }}-addtask-todo-microservice
    spec:
      containers:
      - name: {{ .Values.environment }}-addtask-todo-microservice
        image: k8acrregistry{{ .Values.environment }}.azurecr.io/addaaskaodo:latest
        env:
        - name: CONNECTION_STRING
          value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:{{ .Values.environment }}k8sqlserverc.database.windows.net,1433;Database={{ .Values.environment }}k8db;Uid=sqladmin;Pwd=Password@123;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: {{ .Values.addtask.targetPort }}

