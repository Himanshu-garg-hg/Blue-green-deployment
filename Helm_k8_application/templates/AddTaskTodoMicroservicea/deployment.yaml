apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-addtask-todo-microservice-pod
  labels:
    app: {{ .Release.Name }}-addtask-todo-microservice
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}-addtask-todo-microservice
  replicas: {{ .Values.addtask.replicas }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-addtask-todo-microservice
    spec:
      containers:
      - name: {{ .Release.Name }}-addtask-todo-microservice
        image: k8acrregistry{{ .Release.Name }}.azurecr.io/addaaskaodo:latest
        env:
        - name: CONNECTION_STRING
          value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:k8sqlserverc.database.windows.net,1433;Database=k8db;Uid=sqladmin;Pwd=Password@123;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: {{ .Values.addtask.targetPort }}

