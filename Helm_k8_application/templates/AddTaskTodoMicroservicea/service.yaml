apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.environment }}-addtask-todo-microservice-service
spec:
  selector:
    app: {{ .Values.environment }}-addtask-todo-microservice
  ports:
  - port: {{ .Values.deletetask.port }}
    targetPort: {{ .Values.addtask.targetPort }}
  type: ClusterIP
