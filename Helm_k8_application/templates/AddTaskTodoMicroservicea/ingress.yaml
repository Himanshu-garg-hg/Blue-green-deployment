apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.environment }}-addtask-todo-microservice-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: addtask.tangi.fun
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: {{ .Values.environment }}-addtask-todo-microservice-service
            port: 
              number: {{ .Values.addtask.port }}
