apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-gettasks-todo-microservice-pod
  labels:
    app: {{ .Release.Name }}-gettasks-todo-microservice
spec:
   replicas: {{ .Values.gettask.replicas }}
   selector:
     matchLabels:
        app: {{ .Release.Name }}-gettasks-todo-microservice
   template:
     metadata:
       labels:
          app: {{ .Release.Name }}-gettasks-todo-microservice
     spec:
       containers:
          - name: {{ .Release.Name }}-gettasks-todo-microservice
            image: k8acrregistry{{ .Release.Name }}.azurecr.io/gettaskstodo:latest
            ports:
              - containerPort: {{ .Values.gettask.targetPort }}
            env:
              - name: CONNECTION_STRING
                value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:k8sqlserverc.database.windows.net,1433;Database=k8db;Uid=sqladmin;Pwd=Password@123;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"                                          
            resources:
              requests:
                cpu: "100m"
                memory: "128Mi"
              limits:
                cpu: "250m"
                memory: "256Mi"