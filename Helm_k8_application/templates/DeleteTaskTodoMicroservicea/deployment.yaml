apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.environment }}-deletetask-todo-pod
  labels:
    app: {{ .Values.environment }}-deletetask-todo-microservice
spec:
   replicas: {{ .Values.deletetask.replicas }}
   selector:
      matchLabels:
          app: {{ .Values.environment }}-deletetask-todo-microservice
   template:
     metadata:
       labels:
          app: {{ .Values.environment }}-deletetask-todo-microservice
     spec:
       containers:
          - name: {{ .Values.environment }}-deletetask-todo-microservice
            image: k8acrregistry{{ .Values.environment }}.azurecr.io/deletetasktodo:latest
            ports:
              - containerPort: {{ .Values.deletetask.targetPort }}
            env:
              - name: CONNECTION_STRING
                value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:{{ .Values.environment }}k8sqlserverc.database.windows.net,1433;Database={{ .Values.environment }}k8db;Uid=sqladmin;Pwd=Password@123;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"                                          
            resources:
              requests:
                cpu: "100m"
                memory: "128Mi"
              limits:
                cpu: "250m"
                memory: "256Mi" 